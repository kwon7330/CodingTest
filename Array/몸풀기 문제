#include "iostream"
#include <algorithm>
using namespace std;

int main()
{
	int iSize = 0;
	int iArr[100000] = { 0 };
	cin >> iSize;
	for (int i = 0; i < iSize; i++)
	{
		cin >>iArr[i];
	}

	sort(iArr, iArr+iSize);

	for (int i = 0; i < iSize; i++)
	{
		cout << iArr[i] << " ";
	}
	cout << "\n";

	return 0;
}
